## 1. Migration cleanup (D1)

- [x] 1.1 Hapus `dev-packages/vitamind-core/database/migrations/0001_01_01_000000_create_users_table.php`, `0001_01_01_000001_create_cache_table.php`, `0001_01_01_000002_create_jobs_table.php` — identik dengan skeleton Laravel, tidak ada modifikasi VitaminD
- [x] 1.2 Jalankan `php artisan migrate:fresh` di boilerplate `vitamin-d` ini untuk pastikan migration ALTER (`add_auth_fields_to_users_table`, `add_two_factor_columns_to_users_table`) tetap jalan benar setelah 3 file dihapus, memakai migration `users`/`cache`/`jobs` bawaan yang sudah ada di app-layer — **catatan implementasi**: `database/migrations/` app-layer ternyata kosong (bukan "sudah ada" seperti asumsi tasks.md), jadi 3 migration standar Laravel yang sama ditambahkan ke `database/migrations/` boilerplate ini. Juga ditemukan bug terpisah (di luar D1-D5): `PluginCache::resolvePluginMetadata()` fatal error saat tabel `cache` belum ada (chicken-and-egg pada fresh migrate dengan `CACHE_STORE=database`) — ditambal dengan try/catch fallback, konsisten dengan pola `PluginCache::get()`
- [x] 1.3 Jalankan `php artisan test` untuk pastikan tidak ada test yang bergantung pada migration yang dihapus — 83/83 test lolos

## 2. Config default & merge (D4)

- [x] 2.1 Buat `dev-packages/vitamind-core/config/vitamin-d.php` berisi default untuk semua key `features.*` (workspaces, api, two_factor, websocket, admin_panel), `pagination_size`, dan `plugins.marketplace.*` — sama strukturnya dengan `vitamin-d/config/vitamin-d.php` yang ada sekarang
- [x] 2.2 Tambahkan `$this->mergeConfigFrom(__DIR__.'/../../config/vitamin-d.php', 'vitamin-d')` di `CoreServiceProvider::register()`
- [x] 2.3 (Opsional) Tambahkan `$this->publishes([...], 'vitamin-d-config')` supaya app konsumen bisa `vendor:publish` kalau mau override penuh
- [x] 2.4 Test: hapus sementara `vitamin-d/config/vitamin-d.php` app-layer secara lokal, pastikan `config('vitamin-d.features.workspaces')` masih resolve ke boolean (bukan null) — lalu kembalikan filenya — dikonfirmasi manual via tinker: `bool(true)`, `int(25)`

## 3. Bootstrap & dead code cleanup (D5)

- [x] 3.1 Hapus gate manual `if (config('vitamin-d.features.workspaces')) { $providers[] = WorkspaceServiceProvider::class; }` dari `bootstrap/providers.php`, andalkan auto-discovery Composer
- [x] 3.2 Hapus total file `dev-packages/vitamind-core/src/Providers/AppServiceProvider.php` (tidak terdaftar di manapun, isinya salah)
- [x] 3.3 Verifikasi `php artisan route:list` masih menampilkan route workspace plugin setelah perubahan 3.1 — dikonfirmasi, 10 route `workspaces.*` masih terdaftar via auto-discovery

## 4. Perbaikan hardcode `App\Models\User`/`App\Models\PersonalAccessToken` — Kategori A: type-hint saja (D3)

- [x] 4.1 Ganti `use App\Models\User;` → `use VitaminD\Core\Models\User;` di file yang hanya type-hint/docblock: `helpers.php`, `UserResource.php`, `HandleInertiaRequests.php`, `CreateApiKey.php`, `UpdateUserPassword.php`, `UpdateUserProfileInformation.php`, `UpdateUser.php`, `ResetUserPassword.php`, `UserPolicy.php`
- [x] 4.2 Ganti `use App\Models\User;` → `use VitaminD\Core\Models\User;` di `CoreServiceProvider.php` (`Gate::policy(User::class, ...)`) dan `Admin/UserController.php`/`ProfileController.php`/`RegisteredUserController.php` khusus untuk baris `authorize()`/`Rule::unique()`/`'unique:'.User::class` (BUKAN baris instantiation — lihat grup 5) — **catatan implementasi**: `Admin/UserController.php` juga punya route model binding implisit (`update(User $user)`/`destroy(User $user)`) yang ikut terkena penggantian import ini; dicek konkret nol bug hari ini, risiko masa depan dicatat sebagai OQ4 di design.md
- [x] 4.3 Ganti `use App\Models\PersonalAccessToken;` → `use VitaminD\Core\Models\PersonalAccessToken;` di `ApiKeyResource.php`, `ApiKeyController.php` (baris `authorize()` saja), `PersonalAccessTokenPolicy.php` — **catatan implementasi**: `ApiKeyController::destroy(PersonalAccessToken $apiKey)` juga route model binding, risiko sama, dicatat di OQ4 yang sama
- [x] 4.4 Ganti `use App\Models\User;` → `use VitaminD\Core\Models\User;` di file `vitamind-workspace-plugin`: `WorkspacePolicy.php`, `HasRolePolicies.php`, `WorkspaceResource.php`, `CanSeeWorkspaceMiddleware.php`, `HasWorkspaceMiddleware.php`, `DeleteWorkspace.php`, `GetWorkspaces.php`, `CreateWorkspace.php` — **deviasi**: `UserWorkspace.php` (relasi `belongsTo`) dan `Workspace.php` (relasi `hasManyThrough`) TIDAK memakai import langsung karena hydrate instance baru (risiko sama dengan grup 5) — dipindah ke `config('auth.providers.users.model')` dinamis, lihat design.md
- [x] 4.5 Ganti `use App\Models\PersonalAccessToken;` → `use VitaminD\Core\Models\PersonalAccessToken;` di `CanSeeWorkspaceMiddleware.php`
- [x] 4.6 Jalankan `php artisan test` — 83/83 lolos. Verifikasi tambahan via tinker: Gate/policy resolution benar untuk `App\Models\User`, mixin `HasWorkspaces` utuh, dan simulasi route-binding mengonfirmasi behavior yang dicatat di OQ4

## 5. Perbaikan hardcode — Kategori B: instantiation/query (D3, risiko tertinggi)

- [x] 5.1 (Opsional, lihat OQ2 design.md) Buat helper kecil, mis. `VitaminD\Core\Support\authUserModel(): string` yang mengembalikan `config('auth.providers.users.model')`, dipakai di 4 titik berikut — dibuat di `dev-packages/vitamind-core/src/Support/AuthUserModel.php`, didaftarkan sebagai composer "files" autoload. Juga dipakai untuk `UserWorkspace.php`/`Workspace.php` (task 4.4, lihat deviasi di sana)
- [x] 5.2 `RegisteredUserController.php:40` — ganti `User::create([...])` jadi resolve dinamis lewat model yang dikonfigurasi (`config('auth.providers.users.model')`), pertahankan validasi `Rule::unique(User::class)` dari grup 4
- [x] 5.3 `CreateNewUser.php:37` — ganti `User::create([...])` dengan cara yang sama
- [x] 5.4 `CreateUser.php:28` — ganti `User::query()->create([...])` dengan cara yang sama
- [x] 5.5 `Admin/UserController.php:33,50` — ganti `User::query()->...` (listing & search) dengan cara yang sama
- [x] 5.6 Tambah test: user yang dibuat lewat registrasi & lewat admin adalah instance `App\Models\User` (bukan `VitaminD\Core\Models\User` polos), dan saat fitur workspace aktif, instance itu punya method `workspaces()`/`HasWorkspaces` tanpa perlu re-fetch manual — ditambahkan di `RegistrationTest.php` dan `UserManagementTest.php`
- [x] 5.7 Jalankan `php artisan test` penuh + uji manual jalur registrasi, admin create-user, admin list/search user — 86/86 test lolos. Verifikasi list/search dilakukan via test otomatis (`test_admin_can_search_users`) alih-alih manual browser, lebih presisi & jadi regresi permanen. **Catatan operasional**: sempat terjebak cache metadata `vendor/composer/installed.json` untuk package path-repository — `composer dump-autoload` biasa TIDAK mem-refresh daftar "files" autoload package path-repo yang berubah; perlu `composer update vitamind/core --no-scripts` untuk itu

## 6. Update dokumentasi

- [x] 6.1 `MIGRATION_GUIDE.md` — perbaiki klaim "install core alone, that's it, no User model needed": jelaskan `App\Models\User extends VitaminD\Core\Models\User` WAJIB untuk semua skenario instalasi (bukan cuma saat pakai workspace plugin), karena controller/policy/action inti core bergantung pada method yang hanya ada di kelas itu — ditambahkan sebagai langkah wajib eksplisit sebelum "Verify with", section workspace disesuaikan (User model sudah ada dari langkah dasar, tinggal tambah trait)
- [x] 6.2 `MIGRATION_GUIDE.md` — tambahkan peringatan eksplisit: jangan jalankan `php artisan install:api` berdampingan dengan `vitamind/core` (mencegah kolisi migration `personal_access_tokens`, temuan D2)
- [x] 6.3 `MIGRATION_GUIDE.md` — hapus/koreksi bagian yang menyiratkan `bootstrap/providers.php` perlu diedit manual untuk mengaktifkan workspace plugin; tegaskan cukup `composer require` + `.env` flag — **catatan**: dicek, doc yang ada tidak pernah menyiratkan ini (section workspace sudah sejak awal tidak menyebut bootstrap/providers.php); ditambahkan kalimat eksplisit "no bootstrap/providers.php edit needed here either" untuk penegasan, plus catatan bahwa default config `vitamin-d.php` dari core (D4) membuat flag resolve tanpa app perlu publish config sendiri
- [x] 6.4 `README.md`/`CONTRIBUTING.md` — sinkronkan klaim terkait instalasi fresh Laravel dan User model dengan hasil 6.1–6.3 kalau ada bagian yang menyebut hal serupa — dicek, README.md sudah konsisten (baris `App\Models\User extends VitaminD\Core\Models\User` sudah ada), CONTRIBUTING.md tidak menyebut topik ini sama sekali; tidak ada perubahan diperlukan
- [x] 6.5 Catat temuan sampingan `server-provider`/`dns-provider`/`plugins.views` config yang tidak pernah ada filenya (dari `GetBootstrap.php`) sebagai item terpisah untuk diklarifikasi — bukan bagian dari fix ini (lihat Non-Goals design.md) — dicatat sebagai OQ5 di design.md

## 7. Verifikasi end-to-end & regresi (definition of done)

- [x] 7.1 Buat proyek `laravel new` baru di direktori scratch terpisah, `composer require vitamind/core:@dev` via path repository, `php artisan migrate`, verifikasi `route:list` + `curl /api/health` sesuai langkah `MIGRATION_GUIDE.md` — tanpa membuat `App\Models\User` dulu, konfirmasi bahwa developer memang WAJIB membuatnya (sesuai 6.1) dan tanpa itu terjadi error yang jelas, bukan silent failure — dilakukan via `composer create-project laravel/laravel` (setara `laravel new`, installer CLI tidak tersedia). Migration D1 terkonfirmasi jalan bersih di atas migration bawaan Laravel asli. `route:list`/`curl /api/health` sukses. Tanpa `App\Models\User`: error jelas `TypeError: Return value must be of type VitaminD\Core\Models\User, App\Models\User returned` — bukan silent failure. **2 temuan baru dari verifikasi nyata ini**: (a) `App\Models\PersonalAccessToken` juga wajib dibuat, belum disebut di 6.1 — sudah ditambahkan ke MIGRATION_GUIDE.md; (b) `PersonalAccessTokenPolicy` ternyata tidak pernah didaftarkan via `Gate::policy()` (dicek `git log`, bug pra-existing, bukan dari change ini) — `/settings/api-keys` selalu 403 untuk siapa pun. Di-fix: tambah `Gate::policy(PersonalAccessToken::class, PersonalAccessTokenPolicy::class)` di `CoreServiceProvider::boot()`, plus test regresi baru `tests/Feature/ApiKeyManagementTest.php`
- [x] 7.2 Di proyek scratch yang sama, buat `App\Models\User extends VitaminD\Core\Models\User`, ulangi verifikasi — pastikan registrasi, login, admin panel, 2FA, API key semua berfungsi — dikonfirmasi via tinker: registrasi (`CreateNewUser`), admin create (`CreateUser` + `isAdmin()`), policy Gate resolution, API key (`CreateApiKey` → `NewAccessToken` dengan `plainTextToken`), dan penulisan field 2FA semua sukses setelah `App\Models\User`+`App\Models\PersonalAccessToken` dibuat
- [x] 7.3 Tambahkan `composer require vitamind/workspace-plugin:@dev`, set `VITAMIND_FEATURE_WORKSPACES=true` di `.env`, `php artisan migrate` — konfirmasi tabel workspace benar-benar dibuat dan route aktif (ini yang gagal diam-diam sebelum fix D4) — dikonfirmasi: flag resolve ke `bool(true)` TANPA `config/vitamin-d.php` app-layer sama sekali (D4 bekerja), tabel `workspaces`/`user_workspace` terbuat, 10 route `workspaces.*` aktif. Verifikasi fungsional via tinker: `CreateWorkspace` action sukses, `$user->workspaces()->count()` benar, dan `UserWorkspace->user` relation mengembalikan `App\Models\User` (bukan CoreUser polos) — mengonfirmasi fix resolve dinamis di task 4.4 bekerja end-to-end
- [x] 7.4 Jalankan `php artisan test` penuh di boilerplate `vitamin-d` sebagai regresi akhir — 89/89 lolos (86 dari task sebelumnya + 3 test baru `ApiKeyManagementTest`)
- [x] 7.5 Update `openspec/changes/stabilize-vitamind-packages/tasks.md` §0 dengan catatan bahwa `fix-pre-dogfooding-gaps` sudah selesai, sebagai syarat mulai dogfooding ke BukuWarga/LembarUji/UangKas
