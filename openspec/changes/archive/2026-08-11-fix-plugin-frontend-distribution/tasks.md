## 1. Hapus mekanisme Local plugin yang mati

- [x] 1.1 Hapus `getPluginAliases()` (bagian yang scan `app/Plugins/Local/*`) dari `vite.config.ts`
- [x] 1.2 Hapus cabang resolver `@plugin/` di `resources/js/app.tsx` yang menunjuk `app/Plugins/Local/{vendor}/{plugin}/resources/js/pages/**`
- [x] 1.3 Konfirmasi tidak ada plugin (`HelloWorld`, `MockProduct`) yang bergantung pada mekanisme ini sebelum dihapus (grep `resources/js` di `app/Plugins/*`) — nol hasil, aman dihapus

## 2. Bangun ulang mekanisme `@plugin/{name}` untuk plugin distributed

- [x] 2.1 Tulis ulang `getPluginAliases()` di `vite.config.ts` untuk scan `vendor/vitamind/*/resources/js`, hasilkan alias `@plugin/{kebab-name}` menunjuk ke folder itu
- [x] 2.2 Verifikasi empiris: buat file `.tsx` percobaan di `dev-packages/vitamind-workspace-plugin/resources/js/pages/`, konfirmasi ter-resolve lewat symlink `vendor/vitamind/workspace-plugin` tanpa masalah — terbukti lewat `npm run build`, file percobaan muncul di `public/build/manifest.json` dengan path asli `dev-packages/vitamind-workspace-plugin/...`; risiko symlink+glob Vite 8 di design.md tidak terjadi
- [x] 2.3 Tulis ulang cabang resolver `@plugin/{name}/{page}` di `resources/js/app.tsx` untuk glob `vendor/vitamind/*/resources/js/pages/**`
- [x] 2.4 Hapus file percobaan dari 2.2 setelah verifikasi selesai

## 3. Cakupan Tailwind untuk plugin distributed

- [x] 3.1 Tambah `@source` di `resources/css/app.css` menunjuk `vendor/vitamind` (bentuk direktori polos, mengikuti pola `@source '../views'` yang sudah ada — bentuk glob `*/resources/js/**` sempat dicoba tapi bentuk direktori polos yang terbukti jalan)
- [x] 3.2 Verifikasi: className Tailwind arbitrary-value di file percobaan (`tracking-[0.377em]`) menghasilkan rule CSS nyata (`--tw-tracking:.377em`) di build output

## 4. Interface pemanggilan stabil

- [x] 4.1 Tambah helper `usePlugin(name: string)` di `resources/js/lib/use-plugin.ts`, membaca dari peta eager `import.meta.glob` atas `vendor/vitamind/*/resources/js/**/*.{ts,tsx}` (mengecualikan `pages/**`, yang tetap ditangani resolver lazy terpisah)
- [x] 4.2 `usePlugin()` mengembalikan objek kosong (`byPlugin[name] ?? {}`) kalau plugin tidak ditemukan — tidak ada jalur throw sama sekali di fungsinya
- [x] 4.3 Resolver `@plugin/{name}/{page}` di `app.tsx` dibuat hybrid: peta glob dicoba dulu, fallback ke `window.__VITAMIND_PLUGIN_PAGES__?.[name]` (placeholder kosong, belum ada yang mengisi — disiapkan untuk mekanisme registry mendatang) sebelum throw
- [x] 4.4 Verifikasi: `npm run types` (tsc --noEmit) bersih setelah perubahan; skenario "plugin tidak ditemukan" terverifikasi lewat inspeksi kode — `byPlugin[name] ?? {}` secara struktural tidak punya jalur throw untuk `name` apa pun, tidak perlu test runtime terpisah

## 5. Migrasi retroaktif: `vitamind/workspace-plugin`

- [x] 5.1 Pindahkan `resources/js/pages/workspaces/{index,onboarding}.tsx` dan seluruh `resources/js/pages/workspaces/components/*` ke `dev-packages/vitamind-workspace-plugin/resources/js/pages/` (via `git mv`, riwayat terjaga)
- [x] 5.2 Update `Inertia::render('workspaces/index', ...)` → `'@plugin/workspace-plugin/index'` di `WorkspaceController.php`, dan `'workspaces/onboarding'` → `'@plugin/workspace-plugin/onboarding'` di `WorkspaceOnboardingController.php`
- [x] 5.3 Perbaiki import: (a) `columns.tsx`, `index.tsx`, `users.tsx` di dalam plugin sendiri tadinya pakai `@/pages/workspaces/components/...` (alias host) untuk saling rujuk antar-file plugin — diubah jadi relatif (`./...`); (b) ditemukan satu referensi lolos dari pencarian awal: `resources/js/components/workspace-switch.tsx` (komponen HOST) impor `WorkspaceForm` milik plugin — diubah ke `@plugin/workspace-plugin/pages/components/workspace-form`; (c) ditemukan **tempat ketiga** yang masih menunjuk convention lama `app/Plugins/Local/*`: `tsconfig.json`'s `paths` — dihapus, diganti ambient wildcard `declare module '@plugin/*';` di `resources/js/types/plugin-modules.d.ts` (harus file terpisah tanpa import/export top-level, terbukti lewat eksperimen — taruh di file yang punya import lain seperti `global.d.ts` membuat wildcard-nya tidak lagi ke-detect sebagai ambient global)
- [x] 5.4 Verifikasi manual via `php artisan serve` + curl (login sungguhan, cookie session asli): `/settings/workspaces` dan `/settings/workspaces/onboarding` sama-sama HTTP 200, `data-page` JSON berisi `"component":"@plugin/workspace-plugin/index"` dan `"...onboarding"` dengan benar, asset `<script type="module">` menunjuk file hasil build yang tepat (`pages-*.js`, `onboarding-*.js`), tidak ada exception di response. **Bug ditemukan & diperbaiki dalam proses ini**: `resources/views/app.blade.php`'s `@vite(['resources/js/app.tsx', "resources/js/pages/{$page['component']}.tsx"])` mengasumsikan semua halaman ada di `resources/js/pages/` — salah untuk halaman `@plugin/...` yang kini ada di `vendor/vitamind/{plugin}/resources/js/pages/`. Diverifikasi lewat tinker bahwa `Vite::asset()` melempar `ViteException` untuk path lama. Diperbaiki dengan resolve `realpath()` (pola sama seperti fix symlink di `CoreServiceProvider::registerRoutes()`) sebelum lookup manifest. **Bug kedua ditemukan lewat full test suite** (`php artisan test`, mencakup host + `dev-packages/vitamind-{core,workspace-plugin,plugin-sdk,realtime-plugin}/tests` sesuai `phpunit.xml`): `tests/Feature/WorkspaceInviteOnboardingTest.php` mengasersi `->component('workspaces/onboarding')` (nama lama) dan gagal lagi dengan alasan BERBEDA setelah nama diupdate — Inertia's `assertInertia()->component()` punya pengecekan eksistensi file bawaan (`config('inertia.testing.ensure_pages_exist')`, default true) yang cuma tahu `resources/js/pages/`, tidak tahu convention `@plugin/`. Diperbaiki dengan parameter `$shouldExist=false` bawaan method itu (`->component('@plugin/workspace-plugin/onboarding', false)`) — cuma menonaktifkan pengecekan untuk assertion plugin ini, tidak mengubah config global/tidak melemahkan safety-net untuk halaman host biasa. Setelah kedua fix: `php artisan test` penuh **128/128 lulus**, `vendor/bin/pint` bersih di semua file PHP yang tersentuh.
- [x] 5.5 Verifikasi `pendingInvite`: `resources/js/pages/auth/register.tsx` (tidak dipindah, tidak disentuh) dicek lewat curl — HTTP 200, component `auth/register`, tidak ada error. Logic injeksi `pendingInvite` di `WorkspaceServiceProvider` sendiri tidak disentuh perubahan ini, jadi risikonya sudah dieliminasi di level kode; verifikasi ini konfirmasi tidak ada regresi tidak langsung.

## 6. Migrasi retroaktif: `vitamind/realtime-plugin`

- [x] 6.1 Pindahkan `resources/js/lib/echo.ts`, `resources/js/hooks/use-socket-events.ts`, `resources/js/hooks/use-broadcast-channel.ts`, `resources/js/stores/socket-store.ts` ke `dev-packages/vitamind-realtime-plugin/resources/js/{lib,hooks,stores}/` (via `git mv`); referensi internal antar-file (`@/stores/socket-store`, `@/lib/echo`) diubah jadi relatif (`../stores/socket-store`, `../lib/echo`) — `@/hooks/use-feature` yang dipakai `use-socket-events.ts` TIDAK diubah, itu memang hook host, tetap resolve benar lewat `@` karena plugin & host dikompilasi satu proses Vite
- [x] 6.2 `resources/js/layouts/app/layout.tsx` (komponen `Layout` dan `BootstrapBroadcastListener`) sekarang memanggil `usePlugin('realtime-plugin')` lalu destructure `useSocketEvents`/`useBroadcastChannel`, bukan import langsung. Juga ditemukan & diperbaiki: `resources/js/components/app-header.tsx` mengimpor tipe `SocketStatus` dari `@/hooks/use-socket-events` (path lama) — pindah ke kontrak tipe host sendiri (lihat 6.3)
- [x] 6.3 Fallback no-op (`noopUseSocketEvents` → status `'disconnected'`, `noopUseBroadcastChannel` → no-op) didefinisikan sebagai konstanta module-level di `layout.tsx` (bukan dibuat ulang tiap render — supaya referensi hook stabil lintas render, sesuai rules-of-hooks). **Temuan tambahan**: `import type { SocketStatus }`/`BroadcastEventMap` langsung dari `@plugin/realtime-plugin/...` gagal type-check (`TS2709: Cannot use namespace as a type` — ambient wildcard module kosong tidak bisa membawa named type presisi, dibuktikan lewat reproduksi terisolasi). Diperbaiki dengan cara yang lebih tepat secara arsitektur: dibuat `resources/js/types/realtime-plugin.ts` sebagai kontrak tipe milik HOST sendiri (`SocketStatus`, `RealtimePlugin`), tidak menembus ke tipe internal plugin — `app-header.tsx` dan `layout.tsx` sama-sama impor dari situ
- [x] 6.4 Verifikasi manual via `php artisan serve` + curl (login sungguhan): `/dashboard` (yang pakai `Layout`→`BootstrapBroadcastListener`→`usePlugin('realtime-plugin')`) HTTP 200, tanpa exception. Dikonfirmasi lewat `grep` pada bundle hasil build bahwa logic asli plugin (`mapPusherState`, `state_change` dari `socket-store.ts`) benar-benar ter-bundle ke `layout`/`app` chunk — bukan diam-diam jatuh ke fallback no-op. Indikator visual WiFi di `AppHeader` tidak dicek langsung di browser (tidak ada tool browser di sesi ini) — tapi secara desain indikator itu cuma tampil kalau `features.websocket` true, dan config dev saat ini `websocket:false`, jadi perilakunya sama persis sebelum/sesudah perubahan ini either way.

## 7. Dokumentasi

- [x] 7.1 `docs/local-plugins.md`: tambah bagian "Frontend" eksplisit soal FE Local plugin (tidak ada mekanisme khusus, tulis langsung di `resources/js/pages/...`, arahkan ke sistem `dynamic-page` untuk kebutuhan CRUD)
- [x] 7.2 `docs/local-plugins.md`: tambah langkah baru (step 5) di "Extracting a local plugin into a standalone package" soal migrasi file FE saat ekstraksi, merujuk ke bagian "Distributed plugin frontend"
- [x] 7.3 `MIGRATION_GUIDE.md` baris ~108-110: ditambah catatan blockquote yang menjelaskan pernyataan "Core does not ship views/frontend assets" itu historis (khusus 2 paket asli, FE-nya sudah ikut fork boilerplate), bukan kebijakan permanen — merujuk ke dokumentasi mekanisme baru
- [x] 7.4 `docs/local-plugins.md`: bagian baru "Distributed plugin frontend" — layout folder (`pages/`, `hooks/`, `lib/`, `stores/`), cara PHP me-render halaman (`@plugin/{name}/{page}`), cara host konsumsi hook (`usePlugin()`), catatan kontrak tipe host-sendiri (bukan `import type` dari `@plugin/...`), cakupan Tailwind, dan batasan GitHub-source (belum bisa sampai ada rebuild-on-install)

## 8. Koordinasi eksternal (tidak memblokir merge, tapi jangan hilang)

- [x] 8.1 ~~Konfirmasi ke tim wakuwaku apakah resync mereka menarik file level host...~~ Terjawab: wakuwaku melakukan full resync (termasuk file host) — tidak ada instruksi tempel-manual yang diperlukan
- [x] 8.2 Gate `package-stability-gate` di `stabilize-vitamind-packages` diupdate (proposal.md, design.md, specs/package-stability-gate/spec.md, tasks.md) untuk mencatat wakuwaku sebagai proyek dogfooding ke-4 — threshold dipertahankan sebagai angka absolut ≥2 (bukan dinaikkan ke ≥3 dari 4), dikonfirmasi pemilik proyek. Change tetap valid (`openspec validate`).
